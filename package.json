{
  "name": "nexus-api",
  "version": "1.0.0",
  "description": "Clean and minimalist REST API services",
  "main": "api/v1/tobase64.js",
  "scripts": {
    "start": "vercel dev",
    "dev": "vercel dev",
    "deploy": "vercel --prod",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "rest-api",
    "api",
    "base64",
    "converter"
  ],
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "@vercel/node": "^2.15.3"
  },
  "devDependencies": {
    "vercel": "^32.2.1"
  },
  "engines": {
    "node": ">=16.x"
  }
}